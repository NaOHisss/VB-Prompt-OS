VBPO是由VB6開發的一個命令提示系統。
總的來說，這個命令提示系統就和命令提示符差不多。
但是用法和命令提示符不一樣。
打開后即可看見輸入窗口。裡面可以輸入命令。
命令種類目前較少，但是我會努力開發的。
支持的指令可以通過命令 help 来查看。
但是请注意不要更改system文件夾下的任何內容，否則會引起異常。
命令輸入完畢后，可以通過菜單“運行”->“運行輸入代碼”來執行。
執行結果都在“輸出”窗口裡面。
當然也可以使用快捷鍵Ctrl+R來執行命令。
感謝您的使用。